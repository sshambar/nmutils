
nmg_log_stderr=1
# export show_debug, we use in some test programs
export nmg_show_debug=1

# load common config
TEST_COMMON=${TEST_COMMON-conf/common.conf}
[ -r "$TEST_COMMON" ] && . "$TEST_COMMON"

#DDNS_GLOBAL_DIG_TIMEOUT=30

# locate programs
NMDDNS_HELPER="$SRC_ROOT/sbin/nmddns-helper"
DHSCRIPT="$SRC_ROOT/sbin/dhclient-ipv6-prefix"

# test programs
DHCLIENT="${TEST_DHCLIENT-$TEST_BIN/dhclient-mock}"
NMG_IP="${TEST_IP-$TEST_BIN/ip-mock}"
NMDDNS_NSUPDATE="${TEST_NSUPDATE-$TEST_BIN/nsupdate-mock}"

# just bogus so ignored
NMG_RADVD="radvd-missing"

# for state/pids
RUNDIR="run/nmutils"
[ -d "$RUNDIR" ] || mkdir -p "$RUNDIR"

DDNS_GLOBAL_DIG_OPTIONS="+time=1 +retry=2"
DDNS_GLOBAL_NSUPDATE_TIMEOUT="3"
